name: Verify MCP SDK Version

on:
  push:
    branches: ["main"]
    tags: ["v*"]
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Verify MCP SDK version
        run: ./tools/verify-mcp-sdk-version.sh
